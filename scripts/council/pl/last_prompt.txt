ARCH-1. Native rally readability and response acceptance gap\n   User play evidence reports a held shot key emits several balls and ordinary ball flight/bounce is too fast to observe. RVK-17 is sufficient and unambiguous on input lifecycle: one press/hold/release transaction, and all involved buttons must be released before re-arming; repeated shots while held violate the locked rule. RVK-16 independently requires one serve per serve input and no repeated launch while held.\n\n   RVK-10 and TZL-8 require distinct surface speed/bounce profiles and player-followable flight through bounce at native 160x144, while KDP-1 repeats tracking and no held-input repetition as release gates. However, no locked contract defines measurable response/re-arm observation criteria or a minimum readable flight/bounce duration/tick window; TZL-8 explicitly leaves timing/tuning to acceptance without defining the acceptance threshold. ARCH cannot choose tuning or authorize Builder work.\n\n   Options:\n     A. PL/GD reopen a proposal to add measurable native-resolution readability and input-response acceptance criteria, then promote before implementation tuning — creates a testable release contract.\n     B. Treat the current qualitative KDP-1/TZL-8 manual gate as sufficient and let playtest judgment decide pass/fail — preserves scope but leaves the reported failure adjudication subjective.\n   Recommend: A — the held-input behavior is already a locked violation, but readable speed/response needs measurable criteria before any tuning decision.

[council] Recurring across memory-short.md and memory-new.md — consider graduating to memory-long.md:
- No tennis source or product-spec implementation changes were made.
- User clarified that implementation is not PL's job and asked for a new branch.

Implemented RVK-18 in TennisCore.

- Added 12-tick serve wind-up countdown to `TennisSimulationState`.
- Reset initializes countdown.
- Serve input is ignored during countdown; movement remains locked.
- Valid server swing launches only after countdown reaches zero.
- Held input produces exactly one `serveHit`.
- Updated focused pacing/serve tests.

Validation:

- `swift test --filter TennisSimulationTests`: 12/12 passed
- `swift build`: passed
- Changes remain uncommitted; unrelated worktree changes preserved.

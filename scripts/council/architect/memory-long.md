Serve faults are a distinct deterministic simulation outcome: the simulation owns fixed-point
landing calculation, legality is judged at landing, `serveFault(server:landing:)` identifies
the event and point end, and the receiver wins. Never encode an illegal serve as `netFault`.
